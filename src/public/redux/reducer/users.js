const initialState = {
	data: [],
	isLoading: false,
	isError: false
};

export default (users = (state = initialState, action) => {
	switch (action.type){
		
		default:
            return state;
	}
})